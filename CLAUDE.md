@AGENTS.md

# Claude Code adapter
Use only nearest local guidance for the touched frontend/media surface. Do not scan the full frame image catalog. Verify current Discourse extension/localization APIs from source when touching legacy plugin code. Skills live under `.agents/skills/`.
