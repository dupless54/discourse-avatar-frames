# AI context system
`CURRENT_STATE.md` is the short checkpoint and `WORKFLOW.md` owns cross-model roles. Multi-session work uses only `docs/ai/work/<feature>/{state.md,progress.md,implementation-plan.md}` unless more detail is truly needed. Keep facts, not chat transcripts.
